#include <vector>
#include <algorithm>
#include <utility>
#include <numeric>

class Solution {
public:
    // 目的，寻找最小次数，将nums中所有元素变为1
    // 允许的操作：选择两个元素，将nums[i] nums[j] 中其中一个替换为他们的最大公约数。

    // 返回使数组 nums 中所有元素都等于 1 的 最少 操作次数。如果无法让数组全部变成 1 ，请你返回 -1 。
    // 两个正整数的最大公约数指的是能整除这两个数的最大正整数。
    int minOperations(std::vector<int>& nums) {
        std::gcd()
    }
};